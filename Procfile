web: uvicorn main:app --host 0.0.0.0 --port $PORT
# worker: python -m bot.main
# Бот переехал на VPS (DigitalOcean, май 2026). См. deploy/digitalocean/README.md.
# Cloud Apps preset "FastAPI" эту строку всё равно игнорирует, но для гигиены
# закомментировано — чтобы кто-то по ошибке не включил worker через панель.
